mixin toggle(args = {})
  - const { name, value, checked, text } = args
  label.toggle&attributes(attributes)
    input(
      class='toggle__input',
      type='checkbox',
      name=name,
      value=value,
      checked=(checked === true) ? "checked" : undefined,
    )
    .toggle__text= text
