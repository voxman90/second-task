mixin label(leftLabel, rightLabel)
    label 
        .label&attributes(attributes)
        span(class="label__left") #{leftLabel}
        span(class="label__right") #{rightLabel}
        if block
            block